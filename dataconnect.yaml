specVersion: "v1beta"
serviceId: "test-app"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "test-app-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
